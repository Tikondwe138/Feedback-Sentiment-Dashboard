<UploadScreen@MDScreen>:
    name: "upload"

    MDBoxLayout:
        orientation: "vertical"
        spacing: dp(10)
        padding: dp(20)

        MDLabel:
            text: "Upload Feedback Data"
            halign: "center"
            font_style: "H4"

        # Add more widgets here (file chooser, upload button, etc.)